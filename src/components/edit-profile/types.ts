export type EditProfileType = {
  username: string;
  name: string;
  avatar: string;
  about: string;
};
export interface FormInterface {
  name: string;
  about: string;
  username: string;
  busy: boolean;
  avatar: string;
}
